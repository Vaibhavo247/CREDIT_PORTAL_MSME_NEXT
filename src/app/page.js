import React from "react";
import LoginClient from "./LoginClient";

export const metadata = {
  title: "Secure Login | Suryoday MSME Portal",
  description: "Secure Single Sign-On (SSO) login for Suryoday Bank MSME & FT-Cash Underwriting Management Portal.",
};

export default function LoginPage() {
  return <LoginClient />;
}
